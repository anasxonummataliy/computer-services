services:
  backend:
    build: .
    container_name: repair-service-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/repair_services.db
    restart: always
